# 變動偵測 — 擴充 3 家交易所交易對清單（Kraken／Coinbase Exchange／Upbit）

| 項目 | 值 |
|---|---|
| 來源 | `track-crypto/cex_symbols_ext`（3 個子集合） |
| 比對區間 | `2026-09-01` → `2026-09-02` |
| 改寫 | — |
| 偵測時間 | 2026-09-05T03:31:15.440251+00:00 |

> ℹ️ **措辭說明**：「自清單消失」「新增」「重新出現」「狀態變化」都只是描述『這個項目在這兩份快照裡的狀態』的事實，**不代表任何原因推測**。機器可讀事件型別為 `DELISTED`／`LISTED`／`REAPPEARED`／`STATUS_CHANGED`（型別定義見 `track-crypto/scripts/detect_delistings.py` 檔頭）。本來源含 3 個子集合，各子集合的完整性守門與熔斷各自獨立判定，互不影響（例如某子集合熔斷不會連帶讓其他子集合也不判定）。

## 子集合 `coinbase`

| 項目 | 值 |
|---|---|
| **自清單消失**（實際差集筆數，已寫入事件流為 `DELISTED`） | **0** |
| 新增（實際差集筆數，已寫入事件流為 `LISTED`） | 1 |
| └─ 其中重新出現 | 0 |
| 狀態變化（已寫入 STATUS_CHANGED） | 0 |
| 去重 dup_keys（前日／當日） | 0 / 0 |
| 完整性守門：前日 | 通過（range_check[752,921]，n=836） |
| 完整性守門：當日 | 通過（range_check[752,921]，n=837） |
| 熔斷門檻 | 1.0%，abs_floor=5（換算前日筆數為 8.36 筆；removed 筆數 0，未觸發） |

### 新增（1）

- `CP-USD` — CP/USD

## 子集合 `upbit`

| 項目 | 值 |
|---|---|
| **自清單消失**（實際差集筆數，已寫入事件流為 `DELISTED`） | **0** |
| 新增（實際差集筆數，已寫入事件流為 `LISTED`） | 0 |
| └─ 其中重新出現 | 0 |
| 去重 dup_keys（前日／當日） | 0 / 0 |
| 完整性守門：前日 | 通過（range_check[762,934]，n=847） |
| 完整性守門：當日 | 通過（range_check[762,934]，n=847） |
| 熔斷門檻 | 1.0%，abs_floor=5（換算前日筆數為 8.47 筆；removed 筆數 0，未觸發） |

## 子集合 `kraken`

| 項目 | 值 |
|---|---|
| **自清單消失**（實際差集筆數，已寫入事件流為 `DELISTED`） | **0** |
| 新增（實際差集筆數，已寫入事件流為 `LISTED`） | 2 |
| └─ 其中重新出現 | 0 |
| 狀態變化（已寫入 STATUS_CHANGED） | 1 |
| 去重 dup_keys（前日／當日） | 0 / 0 |
| 完整性守門：前日 | 通過（range_check[1293,1585]，n=1438） |
| 完整性守門：當日 | 通過（range_check[1293,1585]，n=1440） |
| 熔斷門檻 | 1.0%，abs_floor=5（換算前日筆數為 14.38 筆；removed 筆數 0，未觸發） |

### 新增（2）

- `TEAEUR` — TEA/EUR
- `USTABLESUSD` — USTABLES/USD

### 🔁 狀態變化（1，事件型別 `STATUS_CHANGED`）

以下項目主鍵仍在清單中（未消失），但下列欄位的值改變了。只陳述欄位新舊值，不推測原因。

- `XU3O8EUR` (XU3O8/EUR) — status: 'post_only' → 'online'

---

本紀錄由 `track-crypto/scripts/detect_delistings.py` 自動產生（第二階段）。
僅陳述「哪個項目在哪天消失／出現／重新出現／狀態變化」此一事實，**不含任何解讀或評論**。
