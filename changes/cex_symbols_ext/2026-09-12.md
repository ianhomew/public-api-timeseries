# 變動偵測 — 擴充 3 家交易所交易對清單（Kraken／Coinbase Exchange／Upbit）

| 項目 | 值 |
|---|---|
| 來源 | `track-crypto/cex_symbols_ext`（3 個子集合） |
| 比對區間 | `2026-09-11` → `2026-09-12` |
| 改寫 | — |
| 偵測時間 | 2026-09-12T03:32:09.205666+00:00 |

> ℹ️ **措辭說明**：「自清單消失」「新增」「重新出現」「上游改名」「狀態變化」都只是描述『這個項目在這兩份快照裡的狀態』的事實，**不代表任何原因推測**。機器可讀事件型別為 `DELISTED`／`LISTED`／`REAPPEARED`／`RENAMED`／`STATUS_CHANGED`（型別定義見 `track-crypto/scripts/detect_delistings.py` 檔頭）。本來源含 3 個子集合，各子集合的完整性守門與熔斷各自獨立判定，互不影響（例如某子集合熔斷不會連帶讓其他子集合也不判定）。

## 子集合 `coinbase`

| 項目 | 值 |
|---|---|
| **自清單消失**（實際差集筆數，已寫入事件流為 `DELISTED`） | **0** |
| 新增（實際差集筆數，已寫入事件流為 `LISTED`） | 0 |
| └─ 其中重新出現 | 0 |
| 狀態變化（已寫入 STATUS_CHANGED） | 0 |
| 去重 dup_keys（前日／當日） | 0 / 0 |
| 完整性守門：前日 | 通過（range_check[752,921]，n=837） |
| 完整性守門：當日 | 通過（range_check[752,921]，n=837） |
| 熔斷門檻 | 1.0%，abs_floor=5（換算前日筆數為 8.37 筆；removed 筆數 0，未觸發） |

## 子集合 `upbit`

| 項目 | 值 |
|---|---|
| **自清單消失**（實際差集筆數，已寫入事件流為 `DELISTED`） | **0** |
| 新增（實際差集筆數，已寫入事件流為 `LISTED`） | 0 |
| └─ 其中重新出現 | 0 |
| 去重 dup_keys（前日／當日） | 0 / 0 |
| 完整性守門：前日 | 通過（range_check[762,934]，n=853） |
| 完整性守門：當日 | 通過（range_check[762,934]，n=853） |
| 熔斷門檻 | 1.0%，abs_floor=5（換算前日筆數為 8.53 筆；removed 筆數 0，未觸發） |

## 子集合 `kraken`

| 項目 | 值 |
|---|---|
| **自清單消失**（實際差集筆數，已寫入事件流為 `DELISTED`） | **0** |
| 新增（實際差集筆數，已寫入事件流為 `LISTED`） | 0 |
| └─ 其中重新出現 | 0 |
| 狀態變化（已寫入 STATUS_CHANGED） | 40 |
| 去重 dup_keys（前日／當日） | 0 / 0 |
| 完整性守門：前日 | 通過（range_check[1293,1585]，n=1449） |
| 完整性守門：當日 | 通過（range_check[1293,1585]，n=1449） |
| 熔斷門檻 | 1.0%，abs_floor=5（換算前日筆數為 14.49 筆；removed 筆數 0，未觸發） |

### 🔁 狀態變化（40，事件型別 `STATUS_CHANGED`）

以下項目主鍵仍在清單中（未消失），但下列欄位的值改變了。只陳述欄位新舊值，不推測原因。

- `ACAEUR` (ACA/EUR) — status: 'online' → 'cancel_only'
- `ACAUSD` (ACA/USD) — status: 'online' → 'cancel_only'
- `BKSEUR` (BKS/EUR) — status: 'online' → 'cancel_only'
- `BKSUSD` (BKS/USD) — status: 'online' → 'cancel_only'
- `BNCEUR` (BNC/EUR) — status: 'online' → 'cancel_only'
- `BNCUSD` (BNC/USD) — status: 'online' → 'cancel_only'
- `CQTEUR` (CQT/EUR) — status: 'online' → 'cancel_only'
- `CQTUSD` (CQT/USD) — status: 'online' → 'cancel_only'
- `CXTEUR` (CXT/EUR) — status: 'online' → 'cancel_only'
- `CXTUSD` (CXT/USD) — status: 'online' → 'cancel_only'
- `EPTEUR` (EPT/EUR) — status: 'online' → 'cancel_only'
- `EPTUSD` (EPT/USD) — status: 'online' → 'cancel_only'
- `GAIAEUR` (GAIA/EUR) — status: 'online' → 'cancel_only'
- `GAIAUSD` (GAIA/USD) — status: 'online' → 'cancel_only'
- `HDXEUR` (HDX/EUR) — status: 'online' → 'cancel_only'
- `HDXUSD` (HDX/USD) — status: 'online' → 'cancel_only'
- `IREUR` (IR/EUR) — status: 'online' → 'cancel_only'
- `IRUSD` (IR/USD) — status: 'online' → 'cancel_only'
- `JUNOEUR` (JUNO/EUR) — status: 'online' → 'cancel_only'
- `JUNOUSD` (JUNO/USD) — status: 'online' → 'cancel_only'
- `MATEUR` (MAT/EUR) — status: 'online' → 'cancel_only'
- `MATUSD` (MAT/USD) — status: 'online' → 'cancel_only'
- `MEUR` (M/EUR) — status: 'online' → 'cancel_only'
- `MIREUR` (MIR/EUR) — status: 'online' → 'cancel_only'
- `MIRUSD` (MIR/USD) — status: 'online' → 'cancel_only'
- `MULTIEUR` (MULTI/EUR) — status: 'online' → 'cancel_only'
- `MULTIUSD` (MULTI/USD) — status: 'online' → 'cancel_only'
- `MUSD` (M/USD) — status: 'online' → 'cancel_only'
- `RBCUSD` (RBC/USD) — status: 'online' → 'cancel_only'
- `RIZEEUR` (RIZE/EUR) — status: 'online' → 'cancel_only'
- `RIZEUSD` (RIZE/USD) — status: 'online' → 'cancel_only'
- `SBREUR` (SBR/EUR) — status: 'online' → 'cancel_only'
- `SBRUSD` (SBR/USD) — status: 'online' → 'cancel_only'
- `SCAUSD` (SCA/USD) — status: 'online' → 'cancel_only'
- `VANRYEUR` (VANRY/EUR) — status: 'online' → 'cancel_only'
- `VANRYUSD` (VANRY/USD) — status: 'online' → 'cancel_only'
- `VULTEUR` (VULT/EUR) — status: 'online' → 'cancel_only'
- `VULTUSD` (VULT/USD) — status: 'online' → 'cancel_only'
- `XTEREUR` (XTER/EUR) — status: 'online' → 'cancel_only'
- `XTERUSD` (XTER/USD) — status: 'online' → 'cancel_only'

---

本紀錄由 `track-crypto/scripts/detect_delistings.py` 自動產生（第二階段）。
僅陳述「哪個項目在哪天消失／出現／重新出現／被上游改名／狀態變化」此一事實，**不含任何解讀或評論**。
