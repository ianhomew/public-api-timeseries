# 變動偵測 — 交易所幣種層級狀態旗標（Gate／Coinbase Exchange）

| 項目 | 值 |
|---|---|
| 來源 | `track-crypto/cex_currency_status`（2 個子集合） |
| 比對區間 | `2026-09-03` → `2026-09-04` |
| 改寫 | — |
| 偵測時間 | 2026-09-04T03:31:01.485537+00:00 |

> ℹ️ **措辭說明**：「自清單消失」「新增」「重新出現」「狀態變化」都只是描述『這個項目在這兩份快照裡的狀態』的事實，**不代表任何原因推測**。機器可讀事件型別為 `DELISTED`／`LISTED`／`REAPPEARED`／`STATUS_CHANGED`（型別定義見 `track-crypto/scripts/detect_delistings.py` 檔頭）。本來源含 2 個子集合，各子集合的完整性守門與熔斷各自獨立判定，互不影響（例如某子集合熔斷不會連帶讓其他子集合也不判定）。

## 子集合 `gate`

| 項目 | 值 |
|---|---|
| **自清單消失**（實際差集筆數，已寫入事件流為 `DELISTED`） | **0** |
| 新增（實際差集筆數，已寫入事件流為 `LISTED`） | 10 |
| └─ 其中重新出現 | 0 |
| 狀態變化（已寫入 STATUS_CHANGED） | 8 |
| 去重 dup_keys（前日／當日） | 0 / 0 |
| 完整性守門：前日 | 通過（range_check[4938,6057]，n=5525） |
| 完整性守門：當日 | 通過（range_check[4938,6057]，n=5535） |
| 熔斷門檻 | 1.0%，abs_floor=5（換算前日筆數為 55.25 筆；removed 筆數 0，未觸發） |

### 新增（10）

- `DELTA` — Delta
- `GPRO` — GoPro
- `GTLB` — GitLab Inc.
- `INDEX` — The Index
- `LAOPU` — Laopu Gold 06181.HK
- `PLTR3L` — PLTR3xLong
- `PLTR3S` — PLTR3xShort
- `PROLOGUE` — Prologue
- `TEAM` — Atlassian Corporation Plc
- `ZS` — Zscaler

### 🔁 狀態變化（8，事件型別 `STATUS_CHANGED`）

以下項目主鍵仍在清單中（未消失），但下列欄位的值改變了。只陳述欄位新舊值，不推測原因。

- `AINVDA` (Artificial Inu) — withdraw_disabled: True → False
- `CP` (Cluster Protocol) — withdraw_disabled: True → False
- `FONE` (apeonfone) — trade_disabled: True → False
- `MINA` (Mina) — withdraw_disabled: False → True
- `PLTRX3L` (PLTRX3xLong) — trade_disabled: False → True
- `PLTRX3S` (PLTRX3xShort) — trade_disabled: False → True
- `SWAN` (Swan Chain) — withdraw_disabled: False → True
- `WAXP` (WAX) — withdraw_disabled: True → False

## 子集合 `coinbase`

| 項目 | 值 |
|---|---|
| **自清單消失**（實際差集筆數，已寫入事件流為 `DELISTED`） | **0** |
| 新增（實際差集筆數，已寫入事件流為 `LISTED`） | 0 |
| └─ 其中重新出現 | 0 |
| 狀態變化（已寫入 STATUS_CHANGED） | 0 |
| 去重 dup_keys（前日／當日） | 0 / 0 |
| 完整性守門：前日 | 通過（range_check[453,556]，n=505） |
| 完整性守門：當日 | 通過（range_check[453,556]，n=505） |
| 熔斷門檻 | 1.0%，abs_floor=5（換算前日筆數為 5.05 筆；removed 筆數 0，未觸發） |

---

本紀錄由 `track-crypto/scripts/detect_delistings.py` 自動產生（第二階段）。
僅陳述「哪個項目在哪天消失／出現／重新出現／狀態變化」此一事實，**不含任何解讀或評論**。
