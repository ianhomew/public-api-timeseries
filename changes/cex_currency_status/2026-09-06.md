# 變動偵測 — 交易所幣種層級狀態旗標（Gate／Coinbase Exchange）

| 項目 | 值 |
|---|---|
| 來源 | `track-crypto/cex_currency_status`（2 個子集合） |
| 比對區間 | `2026-09-05` → `2026-09-06` |
| 改寫 | — |
| 偵測時間 | 2026-09-08T03:31:49.301586+00:00 |

> ℹ️ **措辭說明**：「自清單消失」「新增」「重新出現」「狀態變化」都只是描述『這個項目在這兩份快照裡的狀態』的事實，**不代表任何原因推測**。機器可讀事件型別為 `DELISTED`／`LISTED`／`REAPPEARED`／`STATUS_CHANGED`（型別定義見 `track-crypto/scripts/detect_delistings.py` 檔頭）。本來源含 2 個子集合，各子集合的完整性守門與熔斷各自獨立判定，互不影響（例如某子集合熔斷不會連帶讓其他子集合也不判定）。

## 子集合 `gate`

| 項目 | 值 |
|---|---|
| **自清單消失**（實際差集筆數，已寫入事件流為 `DELISTED`） | **0** |
| 新增（實際差集筆數，已寫入事件流為 `LISTED`） | 3 |
| └─ 其中重新出現 | 0 |
| 狀態變化（已寫入 STATUS_CHANGED） | 49 |
| 去重 dup_keys（前日／當日） | 0 / 0 |
| 完整性守門：前日 | 通過（range_check[4938,6057]，n=5537） |
| 完整性守門：當日 | 通過（range_check[4938,6057]，n=5540） |
| 熔斷門檻 | 1.0%，abs_floor=5（換算前日筆數為 55.37 筆；removed 筆數 0，未觸發） |

### 新增（3）

- `FATCOIN` — FATCOIN
- `SHROOM` — MUSHROOM
- `STONKS` — Stonks

### 🔁 狀態變化（49，事件型別 `STATUS_CHANGED`）

以下項目主鍵仍在清單中（未消失），但下列欄位的值改變了。只陳述欄位新舊值，不推測原因。

- `ARN` (ARN) — withdraw_disabled: True → False
- `ASK_OLD` (ASK_OLD) — withdraw_disabled: True → False
- `BFIC` (BFIC Mainnet) — withdraw_disabled: True → False
- `BIFI` (BitcoinFile) — withdraw_disabled: True → False
- `BTCARC` (BTCARC) — withdraw_disabled: True → False
- `BTMV2` (BTMV2) — withdraw_disabled: True → False
- `BU` (Bumo) — withdraw_disabled: True → False
- `CMP` (Caduceus Metaverse Protocol) — withdraw_disabled: True → False
- `CRING` (DarwiniaCrab) — withdraw_disabled: True → False
- `CTO` (Coinversation) — withdraw_disabled: True → False
- `DOGEEVM` (DOGEEVM) — withdraw_disabled: True → False
- `DOGE_HT` (DOGE_HT) — withdraw_disabled: True → False
- `ETC0` (ETC0) — withdraw_disabled: True → False
- `EVC` (EvaChain) — withdraw_disabled: True → False
- `FEG_OLD4` (FEG_OLD4) — withdraw_disabled: True → False
- `FIC` (FileCash) — withdraw_disabled: True → False
- `FTN` (Fasttoken) — withdraw_disabled: True → False
- `GALA2` (Gala) — withdraw_disabled: True → False
- `GEMINIS` (GEMINIS) — withdraw_disabled: True → False
- `GMEE_OLD1` (GMEE_OLD1) — withdraw_disabled: True → False
- `GMEE_OLD2` (GMEE_OLD2) — withdraw_disabled: True → False
- `HT0` (HuobiToken) — withdraw_disabled: True → False
- `KAZE` (KAZE) — withdraw_disabled: True → False
- `KPN` (Apron) — withdraw_disabled: True → False
- `KSX` (SherpaX) — withdraw_disabled: True → False
- `KYOTO` (Kyoto) — withdraw_disabled: True → False
- `LAYR` (LAYR) — withdraw_disabled: True → False
- `LRN` (LRN) — withdraw_disabled: True → False
- `LSKOLD` (LSKOLD) — withdraw_disabled: True → False
- `LUK` (LUK) — withdraw_disabled: True → False
- `MATICZKETH` (MATICZKETH) — withdraw_disabled: True → False
- `NAS_ETH` (Nebulas) — withdraw_disabled: True → False
- `NEONLINK` (Neon Link) — withdraw_disabled: True → False
- `NKN_N2` (NKN_N2) — withdraw_disabled: True → False
- `NODLOLD` (NODLOLD) — withdraw_disabled: True → False
- `PKS` (Polkafoundry) — withdraw_disabled: True → False
- `PPS` (Prophet) — withdraw_disabled: True → False
- `RUNE_OLD` (RUNE_OLD) — withdraw_disabled: True → False
- `STRAXV1` (STRAXV1) — withdraw_disabled: True → False
- `SUBG` (SubGame) — withdraw_disabled: True → False
- `SUPREVM` (SUPREVM) — withdraw_disabled: True → False
- `SUPV1` (SUPV1) — withdraw_disabled: True → False
- `SUPV2` (SUPV2) — withdraw_disabled: True → False
- `TKY` (TKY) — withdraw_disabled: True → False
- `TUR` (TUR) — withdraw_disabled: True → False
- `UNIQUE` (UNIQUE) — withdraw_disabled: True → False
- `VITE` (VITE) — withdraw_disabled: True → False
- `XRT` (XRT) — withdraw_disabled: True → False
- `牛来` (牛来) — withdraw_disabled: True → False

## 子集合 `coinbase`

| 項目 | 值 |
|---|---|
| **自清單消失**（實際差集筆數，已寫入事件流為 `DELISTED`） | **0** |
| 新增（實際差集筆數，已寫入事件流為 `LISTED`） | 0 |
| └─ 其中重新出現 | 0 |
| 狀態變化（已寫入 STATUS_CHANGED） | 0 |
| 去重 dup_keys（前日／當日） | 0 / 0 |
| 完整性守門：前日 | 通過（range_check[453,556]，n=505） |
| 完整性守門：當日 | 通過（range_check[453,556]，n=505） |
| 熔斷門檻 | 1.0%，abs_floor=5（換算前日筆數為 5.05 筆；removed 筆數 0，未觸發） |

---

本紀錄由 `track-crypto/scripts/detect_delistings.py` 自動產生（第二階段）。
僅陳述「哪個項目在哪天消失／出現／重新出現／狀態變化」此一事實，**不含任何解讀或評論**。
