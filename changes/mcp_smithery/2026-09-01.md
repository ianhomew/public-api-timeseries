# 變動偵測 — Smithery MCP 註冊表全量伺服器清單（registry.smithery.ai/servers）

| 項目 | 值 |
|---|---|
| 來源 | `track-crypto/mcp_smithery`（1 個子集合） |
| 比對區間 | `2026-08-31` → `2026-09-01` |
| 改寫 | — |
| 偵測時間 | 2026-09-11T03:32:20.140387+00:00 |

> ℹ️ **措辭說明**：「自清單消失」「新增」「重新出現」「上游改名」「狀態變化」都只是描述『這個項目在這兩份快照裡的狀態』的事實，**不代表任何原因推測**。機器可讀事件型別為 `DELISTED`／`LISTED`／`REAPPEARED`／`RENAMED`／`STATUS_CHANGED`（型別定義見 `track-crypto/scripts/detect_delistings.py` 檔頭）。本來源含 1 個子集合，各子集合的完整性守門與熔斷各自獨立判定，互不影響（例如某子集合熔斷不會連帶讓其他子集合也不判定）。

## 子集合 `_servers`

| 項目 | 值 |
|---|---|
| **自清單消失**（實際差集筆數，僅供人工參考，非正式事件） | **0**（不判定，未寫入事件流） |
| 新增（實際差集筆數，僅供人工參考，非正式事件） | 0（不判定，未寫入事件流） |
| └─ 其中重新出現 | 0（不判定，未寫入事件流） |
| 狀態變化（僅供人工參考） | 0（不判定，未寫入事件流） |
| 去重 dup_keys（前日／當日） | 0 / 0 |
| 完整性守門：前日 | **未通過**（is_full(False) 非布林 True（缺失/False 一律 fail-closed 視為不完整），n=271） |
| 完整性守門：當日 | **未通過**（is_full(False) 非布林 True（缺失/False 一律 fail-closed 視為不完整），n=271） |
| 熔斷門檻 | 1.0%，abs_floor=20（換算前日筆數為 20.00 筆；removed 筆數 0，未觸發） |

> ⚠️ **因完整性守門未通過，本日子集合 `_servers` 不做「自清單消失／新增」判定，`events.jsonl` 未寫入任何事件。** 上表筆數僅為程式算出的原始差集，**未經完整性驗證，不代表正式判定**。

---

本紀錄由 `track-crypto/scripts/detect_delistings.py` 自動產生（第二階段）。
僅陳述「哪個項目在哪天消失／出現／重新出現／被上游改名／狀態變化」此一事實，**不含任何解讀或評論**。
