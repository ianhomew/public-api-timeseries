# 變動偵測 — OpenRouter 全模型清單與定價

| 項目 | 值 |
|---|---|
| 來源 | `track-crypto/openrouter_models`（1 個子集合） |
| 比對區間 | `2026-08-28` → `2026-08-29` |
| 改寫 | — |
| 偵測時間 | 2026-09-05T03:31:18.175091+00:00 |

> ℹ️ **措辭說明**：「自清單消失」「新增」「重新出現」「狀態變化」都只是描述『這個項目在這兩份快照裡的狀態』的事實，**不代表任何原因推測**。機器可讀事件型別為 `DELISTED`／`LISTED`／`REAPPEARED`／`STATUS_CHANGED`（型別定義見 `track-crypto/scripts/detect_delistings.py` 檔頭）。本來源含 1 個子集合，各子集合的完整性守門與熔斷各自獨立判定，互不影響（例如某子集合熔斷不會連帶讓其他子集合也不判定）。

## 子集合 `_models`

| 項目 | 值 |
|---|---|
| **自清單消失**（實際差集筆數，已寫入事件流為 `DELISTED`） | **2** |
| 新增（實際差集筆數，已寫入事件流為 `LISTED`） | 11 |
| └─ 其中重新出現 | 0 |
| 狀態變化（已寫入 STATUS_CHANGED） | 1 |
| 去重 dup_keys（前日／當日） | 0 / 0 |
| 完整性守門：前日 | 通過（total_match，n=387） |
| 完整性守門：當日 | 通過（total_match，n=396） |
| 熔斷門檻 | 3.0%，abs_floor=5（換算前日筆數為 11.61 筆；removed 筆數 2，未觸發） |

### ⚠️ 自清單消失（2）

- `allenai/olmo-3-32b-think` — AllenAI: Olmo 3 32B Think
- `arcee-ai/virtuoso-large` — Arcee AI: Virtuoso Large

### 新增（11）

- `deepseek/deepseek-v4-flash-0731:batch` — DeepSeek: DeepSeek V4 Flash 0731 (batch)
- `deepseek/deepseek-v4-pro-0813:batch` — DeepSeek: DeepSeek V4 Pro 0813 (batch)
- `google/gemma-4-31b-it:batch` — Google: Gemma 4 31B (batch)
- `meta/muse-glimmer-30b:batch` — Meta: Muse Glimmer 30B (batch)
- `moonshotai/kimi-k3:batch` — MoonshotAI: Kimi K3 (batch)
- `openai/gpt-oss-120b:batch` — OpenAI: gpt-oss-120b (batch)
- `openai/gpt-oss-20b:batch` — OpenAI: gpt-oss-20b (batch)
- `qwen/qwen3.5-9b:batch` — Qwen: Qwen3.5-9B (batch)
- `qwen/qwen3.8-2.4t-a95b:batch` — Qwen: Qwen3.8 2.4T A95B (batch)
- `thinkingmachines/inkling-small:batch` — Thinking Machines: Inkling Small (batch)
- `z-ai/glm-5.3-flash:batch` — Z.ai: GLM 5.3 Flash (batch)

### 🔁 狀態變化（1，事件型別 `STATUS_CHANGED`）

以下項目主鍵仍在清單中（未消失），但下列欄位的值改變了。只陳述欄位新舊值，不推測原因。

- `~z-ai/glm-latest` (Z.ai: GLM Latest) — expiration_date: '2098-12-31' → None

---

本紀錄由 `track-crypto/scripts/detect_delistings.py` 自動產生（第二階段）。
僅陳述「哪個項目在哪天消失／出現／重新出現／狀態變化」此一事實，**不含任何解讀或評論**。
