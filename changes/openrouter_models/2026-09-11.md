# 變動偵測 — OpenRouter 全模型清單與定價

| 項目 | 值 |
|---|---|
| 來源 | `track-crypto/openrouter_models`（1 個子集合） |
| 比對區間 | `2026-09-10` → `2026-09-11` |
| 改寫 | — |
| 偵測時間 | 2026-09-12T03:32:16.023856+00:00 |

> ℹ️ **措辭說明**：「自清單消失」「新增」「重新出現」「上游改名」「狀態變化」都只是描述『這個項目在這兩份快照裡的狀態』的事實，**不代表任何原因推測**。機器可讀事件型別為 `DELISTED`／`LISTED`／`REAPPEARED`／`RENAMED`／`STATUS_CHANGED`（型別定義見 `track-crypto/scripts/detect_delistings.py` 檔頭）。本來源含 1 個子集合，各子集合的完整性守門與熔斷各自獨立判定，互不影響（例如某子集合熔斷不會連帶讓其他子集合也不判定）。

## 子集合 `_models`

| 項目 | 值 |
|---|---|
| **自清單消失**（實際差集筆數，已寫入事件流為 `DELISTED`） | **0** |
| 新增（實際差集筆數，已寫入事件流為 `LISTED`） | 7 |
| └─ 其中重新出現 | 5 |
| 狀態變化（已寫入 STATUS_CHANGED） | 1 |
| 去重 dup_keys（前日／當日） | 0 / 0 |
| 完整性守門：前日 | 通過（total_match，n=430） |
| 完整性守門：當日 | 通過（total_match，n=437） |
| 熔斷門檻 | 3.0%，abs_floor=5（換算前日筆數為 12.90 筆；removed 筆數 0，未觸發） |

### 新增（7）（含 5 筆重新出現，詳見下一節）

- `deepseek/deepseek-v4.1-flash` — DeepSeek: DeepSeek V4.1 Flash
- `inclusionai/ling-3.0-flash-vl:free` — inclusionAI: Ling 3.0 Flash VL (free)
- `mistralai/codestral-2508:batch` — Mistral: Codestral 2508 (batch)
- `mistralai/ministral-8b-2512:batch` — Mistral: Ministral 3 8B 2512 (batch)
- `mistralai/mistral-large-2512:batch` — Mistral: Mistral Large 3 2512 (batch)
- `mistralai/mistral-medium-3.1:batch` — Mistral: Mistral Medium 3.1 (batch)
- `mistralai/mistral-small-2603:batch` — Mistral: Mistral Small 4 (batch)

### 🔁 重新出現（5，事件型別 `REAPPEARED`）

- `mistralai/codestral-2508:batch`（先前於 `2026-09-02` 記為自清單消失） — Mistral: Codestral 2508 (batch)
- `mistralai/ministral-8b-2512:batch`（先前於 `2026-09-02` 記為自清單消失） — Mistral: Ministral 3 8B 2512 (batch)
- `mistralai/mistral-large-2512:batch`（先前於 `2026-09-02` 記為自清單消失） — Mistral: Mistral Large 3 2512 (batch)
- `mistralai/mistral-medium-3.1:batch`（先前於 `2026-09-02` 記為自清單消失） — Mistral: Mistral Medium 3.1 (batch)
- `mistralai/mistral-small-2603:batch`（先前於 `2026-09-02` 記為自清單消失） — Mistral: Mistral Small 4 (batch)

### 🔁 狀態變化（1，事件型別 `STATUS_CHANGED`）

以下項目主鍵仍在清單中（未消失），但下列欄位的值改變了。只陳述欄位新舊值，不推測原因。

- `z-ai/glm-5.3-flash` (Z.ai: GLM 5.3 Flash) — expiration_date: None → '2098-12-31'

---

本紀錄由 `track-crypto/scripts/detect_delistings.py` 自動產生（第二階段）。
僅陳述「哪個項目在哪天消失／出現／重新出現／被上游改名／狀態變化」此一事實，**不含任何解讀或評論**。
