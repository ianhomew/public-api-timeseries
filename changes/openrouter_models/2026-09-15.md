# 變動偵測 — OpenRouter 全模型清單與定價

| 項目 | 值 |
|---|---|
| 來源 | `track-crypto/openrouter_models`（1 個子集合） |
| 比對區間 | `2026-09-14` → `2026-09-15` |
| 改寫 | — |
| 偵測時間 | 2026-09-15T03:32:33.682758+00:00 |

> ℹ️ **措辭說明**：「自清單消失」「新增」「重新出現」「上游改名」「狀態變化」都只是描述『這個項目在這兩份快照裡的狀態』的事實，**不代表任何原因推測**。機器可讀事件型別為 `DELISTED`／`LISTED`／`REAPPEARED`／`RENAMED`／`STATUS_CHANGED`（型別定義見 `track-crypto/scripts/detect_delistings.py` 檔頭）。本來源含 1 個子集合，各子集合的完整性守門與熔斷各自獨立判定，互不影響（例如某子集合熔斷不會連帶讓其他子集合也不判定）。

## 子集合 `_models`

| 項目 | 值 |
|---|---|
| **自清單消失**（實際差集筆數，已寫入事件流為 `DELISTED`） | **2** |
| 新增（實際差集筆數，已寫入事件流為 `LISTED`） | 2 |
| └─ 其中重新出現 | 0 |
| 狀態變化（已寫入 STATUS_CHANGED） | 0 |
| 去重 dup_keys（前日／當日） | 0 / 0 |
| 完整性守門：前日 | 通過（total_match，n=445） |
| 完整性守門：當日 | 通過（total_match，n=445） |
| 熔斷門檻 | 3.0%，abs_floor=5（換算前日筆數為 13.35 筆；removed 筆數 2，未觸發） |

### ⚠️ 自清單消失（2）

- `google/gemini-2.5-pro-preview-05-06` — Google: Gemini 2.5 Pro Preview 05-06
- `openai/gpt-4-turbo-preview` — OpenAI: GPT-4 Turbo Preview

### 新增（2）

- `~deepseek/deepseek-flash-latest` — DeepSeek: DeepSeek Flash Latest
- `~deepseek/deepseek-pro-latest` — DeepSeek: DeepSeek Pro Latest

---

本紀錄由 `track-crypto/scripts/detect_delistings.py` 自動產生（第二階段）。
僅陳述「哪個項目在哪天消失／出現／重新出現／被上游改名／狀態變化」此一事實，**不含任何解讀或評論**。
