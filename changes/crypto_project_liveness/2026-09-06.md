# 變動偵測 — DefiLlama 駭客事件清單（死亡監控資料面）

| 項目 | 值 |
|---|---|
| 來源 | `track-crypto/crypto_project_liveness`（1 個子集合） |
| 比對區間 | `2026-09-05` → `2026-09-06` |
| 改寫 | — |
| 偵測時間 | 2026-09-07T03:31:43.106389+00:00 |

> ℹ️ **措辭說明**：「自清單消失」「新增」「重新出現」「狀態變化」都只是描述『這個項目在這兩份快照裡的狀態』的事實，**不代表任何原因推測**。機器可讀事件型別為 `DELISTED`／`LISTED`／`REAPPEARED`／`STATUS_CHANGED`（型別定義見 `track-crypto/scripts/detect_delistings.py` 檔頭）。本來源含 1 個子集合，各子集合的完整性守門與熔斷各自獨立判定，互不影響（例如某子集合熔斷不會連帶讓其他子集合也不判定）。

## 子集合 `_hacks`

| 項目 | 值 |
|---|---|
| **自清單消失**（實際差集筆數，已寫入事件流為 `DELISTED`） | **2** |
| 新增（實際差集筆數，已寫入事件流為 `LISTED`） | 4 |
| └─ 其中重新出現 | 0 |
| 去重 dup_keys（前日／當日） | 3 / 3 |
| 完整性守門：前日 | 通過（total_match，n=1253） |
| 完整性守門：當日 | 通過（total_match，n=1255） |
| 熔斷門檻 | 1.0%，abs_floor=5（換算前日筆數為 12.50 筆；removed 筆數 2，未觸發） |

### ⚠️ 自清單消失（2）

- `stake dao1773273600` — Stake DAO
- `stake dao1779840000` — Stake DAO

### 新增（4）

- `dream health chain1788566400` — Dream Health Chain
- `reddio redsonic1788566400` — Reddio RedSonic
- `stake dao yield1773273600` — Stake DAO Yield
- `stake dao yield1779840000` — Stake DAO Yield

---

本紀錄由 `track-crypto/scripts/detect_delistings.py` 自動產生（第二階段）。
僅陳述「哪個項目在哪天消失／出現／重新出現／狀態變化」此一事實，**不含任何解讀或評論**。
